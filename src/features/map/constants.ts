/** Refresh current location once a minute rather than continuously watching GPS, to conserve battery. */
export const LOCATION_POLL_INTERVAL_MS = 60_000;

/** How many recent camera-roll photos to offer in the pin photo picker. */
export const RECENT_PHOTOS_LIMIT = 30;
